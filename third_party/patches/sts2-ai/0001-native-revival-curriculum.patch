diff --git a/STS2AI/ENV/Sim/HeadlessSim/Program.cs b/STS2AI/ENV/Sim/HeadlessSim/Program.cs
index ae63755..4bf3d9d 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Program.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Program.cs
@@ -806,7 +806,9 @@ internal static class Program
 			"play_card" => CombatTrainingActionType.PlayCard,
 			"end_turn" => CombatTrainingActionType.EndTurn,
 			"select_hand_card" => CombatTrainingActionType.SelectHandCard,
+			"deselect_hand_card" => CombatTrainingActionType.DeselectHandCard,
 			"select_card_option" => CombatTrainingActionType.SelectCardChoice,
+			"deselect_card_option" => CombatTrainingActionType.DeselectCardChoice,
 			"confirm_selection" => CombatTrainingActionType.ConfirmSelection,
 			"cancel_selection" => CombatTrainingActionType.CancelSelection,
 			"use_potion" => CombatTrainingActionType.UsePotion,
@@ -817,7 +819,7 @@ internal static class Program
 		if (chosenIdx >= 0)
 		{
 			// select_card_option 走 ChoiceIndex;其他 (play_card/select_hand_card)走 HandIndex
-			if (type == CombatTrainingActionType.SelectCardChoice)
+			if (type is CombatTrainingActionType.SelectCardChoice or CombatTrainingActionType.DeselectCardChoice)
 			{
 				req.ChoiceIndex = chosenIdx;
 			}
@@ -1930,7 +1932,9 @@ internal static class Program
 			"play_card" => CombatTrainingActionType.PlayCard,
 			"end_turn" => CombatTrainingActionType.EndTurn,
 			"select_hand_card" => CombatTrainingActionType.SelectHandCard,
+			"deselect_hand_card" => CombatTrainingActionType.DeselectHandCard,
 			"select_card_option" => CombatTrainingActionType.SelectCardChoice,
+			"deselect_card_option" => CombatTrainingActionType.DeselectCardChoice,
 			"confirm_selection" => CombatTrainingActionType.ConfirmSelection,
 			"cancel_selection" => CombatTrainingActionType.CancelSelection,
 			"use_potion" => CombatTrainingActionType.UsePotion,
@@ -2035,7 +2039,7 @@ internal static class Program
 			})
 			.ToList();
 
-		// cards：id / type / cost / rarity / target_type / tags / keywords / gains_block
+		// cards: exact runtime facts used by the model-facing card-facts ABI.
 		List<Dictionary<string, object?>> cards = ModelDb.AllCards
 			.OrderBy(static c => c.Id.Entry, StringComparer.Ordinal)
 			.Select(static c => new Dictionary<string, object?>
@@ -2049,8 +2053,27 @@ internal static class Program
 				["base_cost"] = SafeBaseCost(c),
 				["is_x_cost"] = c.EnergyCost?.GetType().Name.Contains("XCost") ?? false,
 				["gains_block"] = c.GainsBlock,
+				["has_turn_end_in_hand_effect"] = c.HasTurnEndInHandEffect,
+				["has_on_draw_effect"] = CombatTrainingChoiceSnapshotBuilder.HasOnDrawEffect(c),
+				["exhaust_on_next_play"] = c.ExhaustOnNextPlay,
 				["tags"] = c.Tags.Select(t => t.ToString()).OrderBy(s => s, StringComparer.Ordinal).ToList(),
 				["keywords"] = c.CanonicalKeywords.Select(k => k.ToString()).OrderBy(s => s, StringComparer.Ordinal).ToList(),
+				["hover_tip_ids"] = CombatTrainingChoiceSnapshotBuilder.SafeCardHoverTipIds(c),
+				["dynamic_vars"] = CombatTrainingChoiceSnapshotBuilder.BuildDynamicVarSnapshots(c)
+					.Select(static value => new Dictionary<string, object?>
+					{
+						["name"] = value.Name,
+						["var_type"] = value.VarType,
+						["family"] = value.Family,
+						["power_type"] = value.PowerType,
+						["value_props"] = value.ValueProps,
+						["base_value"] = value.BaseValue,
+						["enchanted_value"] = value.EnchantedValue,
+						["preview_value"] = value.PreviewValue,
+						["int_value"] = value.IntValue,
+						["was_just_upgraded"] = value.WasJustUpgraded
+					})
+					.ToList(),
 			})
 			.ToList();
 
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateBuilder.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateBuilder.cs
index 76abc62..39d8e45 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateBuilder.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateBuilder.cs
@@ -46,6 +46,9 @@ public static class FullRunApiStateBuilder
 			backend_kind = snapshot.BackendKind,
 			coverage_tier = snapshot.CoverageTier,
 			run_outcome = snapshot.RunOutcome,
+			training_revival_budget = TrainingRevivalController.RevivalBudget,
+			training_revivals_used = TrainingRevivalController.RevivalsUsed,
+			training_player_hp_lost = TrainingRevivalController.PlayerHpLost,
 			run = new FullRunApiRun
 			{
 				character_id = snapshot.CharacterId,
@@ -464,6 +467,9 @@ public static class FullRunApiStateBuilder
 			min_select = selection.MinSelect,
 			max_select = selection.MaxSelect,
 			can_confirm = selection.CanConfirm,
+			can_cancel = selection.Cancelable,
+			selected_count = selection.SelectedCards.Count,
+			remaining_select = Math.Max(0, selection.MaxSelect - selection.SelectedCards.Count),
 			cards = selection.SelectableCards.Select(ToApiHandCardOption).ToList(),
 			selected_cards = selection.SelectedCards.Select(ToApiHandCardOption).ToList()
 		};
@@ -479,6 +485,8 @@ public static class FullRunApiStateBuilder
 			max_select = selection.MaxSelect,
 			can_confirm = selection.CanConfirm,
 			can_cancel = selection.Cancelable,
+			selected_count = selection.SelectedCards.Count,
+			remaining_select = Math.Max(0, selection.MaxSelect - selection.SelectedCards.Count),
 			selectable_cards = selection.SelectableCards.Select(ToApiSelectableCardOption).ToList(),
 			selected_cards = selection.SelectedCards.Select(ToApiSelectableCardOption).ToList()
 		};
@@ -626,6 +634,8 @@ public static class FullRunApiStateBuilder
 			col = action.Col,
 			row = action.Row,
 			card_index = action.CardIndex,
+			selection_operation = action.SelectionOperation,
+			is_selected = action.IsSelected,
 			card_id = action.CardId,
 			card_type = action.CardType,
 			card_rarity = action.CardRarity,
@@ -654,6 +664,8 @@ public static class FullRunApiStateBuilder
 			col = action.col,
 			row = action.row,
 			card_index = action.card_index,
+			selection_operation = action.selection_operation,
+			is_selected = action.is_selected,
 			card_id = action.card_id,
 			card_type = action.card_type,
 			card_rarity = action.card_rarity,
@@ -695,7 +707,9 @@ public static class FullRunApiStateBuilder
 			name = SafeGetText(() => relic.Title),
 			rarity = relic.Rarity.ToString(),
 			description = SafeGetText(() => relic.DynamicDescription),
-			counter = relic.ShowCounter ? relic.DisplayAmount : null
+			counter = relic.ShowCounter ? relic.DisplayAmount : null,
+			is_used_up = relic.IsUsedUp,
+			status = relic.Status.ToString()
 		};
 	}
 
@@ -709,10 +723,20 @@ public static class FullRunApiStateBuilder
 			type = card.Type.ToString(),
 			rarity = card.Rarity.ToString(),
 			cost = card.EnergyCost.GetWithModifiers(CostModifiers.All),
+			costs_x = card.EnergyCost.CostsX,
+			star_cost = SafeValue(() => card.GetStarCostWithModifiers(), card.CurrentStarCost),
+			has_star_cost_x = card.HasStarCostX,
 			is_upgraded = card.IsUpgraded,
 			target_type = card.TargetType.ToString(),
 			description = GetCardDescription(card),
-			keywords = card.Keywords.Select(static keyword => keyword.ToString()).ToList()
+			keywords = card.Keywords.Select(static keyword => keyword.ToString()).ToList(),
+			tags = CombatTrainingChoiceSnapshotBuilder.SafeCardTags(card),
+			hover_tip_ids = CombatTrainingChoiceSnapshotBuilder.SafeCardHoverTipIds(card),
+			gains_block = card.GainsBlock,
+			has_turn_end_in_hand_effect = card.HasTurnEndInHandEffect,
+			has_on_draw_effect = CombatTrainingChoiceSnapshotBuilder.HasOnDrawEffect(card),
+			exhaust_on_next_play = card.ExhaustOnNextPlay,
+			dynamic_vars = ToApiDynamicVars(CombatTrainingChoiceSnapshotBuilder.BuildDynamicVarSnapshots(card))
 		};
 	}
 
@@ -770,12 +794,22 @@ public static class FullRunApiStateBuilder
 			name = card.Title,
 			type = card.CardType,
 			cost = card.EnergyCost,
+			costs_x = card.CostsX,
+			star_cost = card.StarCost,
+			has_star_cost_x = card.HasStarCostX,
 			is_upgraded = card.IsUpgraded,
 			can_play = card.CanPlay,
 			target_type = SafeValue(() => card.TargetType.ToString(), string.Empty),
 			description = card.Description,
 			valid_target_ids = card.ValidTargetIds?.ToList() ?? new List<uint>(),
-			keywords = card.Keywords?.ToList() ?? new List<string>()
+			keywords = card.Keywords?.ToList() ?? new List<string>(),
+			tags = card.Tags?.ToList() ?? new List<string>(),
+			hover_tip_ids = card.HoverTipIds?.ToList() ?? new List<string>(),
+			gains_block = card.GainsBlock,
+			has_turn_end_in_hand_effect = card.HasTurnEndInHandEffect,
+			has_on_draw_effect = card.HasOnDrawEffect,
+			exhaust_on_next_play = card.ExhaustOnNextPlay,
+			dynamic_vars = ToApiDynamicVars(card.DynamicVars)
 		};
 	}
 
@@ -788,11 +822,39 @@ public static class FullRunApiStateBuilder
 			name = card.Title,
 			type = SafeValue(() => card.Type.ToString(), string.Empty),
 			cost = card.EnergyCost,
+			costs_x = card.CostsX,
+			star_cost = card.StarCost,
+			has_star_cost_x = card.HasStarCostX,
 			is_upgraded = card.IsUpgraded,
-			target_type = SafeValue(() => card.TargetType.ToString(), string.Empty)
+			target_type = SafeValue(() => card.TargetType.ToString(), string.Empty),
+			keywords = card.Keywords?.ToList() ?? new List<string>(),
+			tags = card.Tags?.ToList() ?? new List<string>(),
+			hover_tip_ids = card.HoverTipIds?.ToList() ?? new List<string>(),
+			gains_block = card.GainsBlock,
+			has_turn_end_in_hand_effect = card.HasTurnEndInHandEffect,
+			has_on_draw_effect = card.HasOnDrawEffect,
+			exhaust_on_next_play = card.ExhaustOnNextPlay,
+			dynamic_vars = ToApiDynamicVars(card.DynamicVars)
 		};
 	}
 
+	private static List<FullRunApiDynamicVar> ToApiDynamicVars(IEnumerable<CombatTrainingDynamicVarSnapshot>? dynamicVars)
+	{
+		return dynamicVars?.Select(static dynamicVar => new FullRunApiDynamicVar
+		{
+			name = dynamicVar.Name,
+			var_type = dynamicVar.VarType,
+			family = dynamicVar.Family,
+			power_type = dynamicVar.PowerType,
+			value_props = dynamicVar.ValueProps,
+			base_value = dynamicVar.BaseValue,
+			enchanted_value = dynamicVar.EnchantedValue,
+			preview_value = dynamicVar.PreviewValue,
+			int_value = dynamicVar.IntValue,
+			was_just_upgraded = dynamicVar.WasJustUpgraded
+		}).ToList() ?? new List<FullRunApiDynamicVar>();
+	}
+
 	private static string RewardTypeForUi(Reward reward)
 	{
 		return reward switch
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateDtos.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateDtos.cs
index 15bb206..7c0bcf5 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateDtos.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateDtos.cs
@@ -14,6 +14,10 @@ public sealed class FullRunApiAction
 
 	public int? card_index { get; set; }
 
+	public string? selection_operation { get; set; }
+
+	public bool? is_selected { get; set; }
+
 	public int? slot { get; set; }
 
 	public uint? target_id { get; set; }
@@ -105,6 +109,29 @@ public sealed class FullRunApiIntent
 	public int? repeats { get; set; }
 }
 
+public sealed class FullRunApiDynamicVar
+{
+	public string? name { get; set; }
+
+	public string? var_type { get; set; }
+
+	public string? family { get; set; }
+
+	public string? power_type { get; set; }
+
+	public string? value_props { get; set; }
+
+	public decimal base_value { get; set; }
+
+	public decimal enchanted_value { get; set; }
+
+	public decimal preview_value { get; set; }
+
+	public int int_value { get; set; }
+
+	public bool was_just_upgraded { get; set; }
+}
+
 public sealed class FullRunApiCardOption
 {
 	public int index { get; set; }
@@ -119,6 +146,12 @@ public sealed class FullRunApiCardOption
 
 	public int? cost { get; set; }
 
+	public bool costs_x { get; set; }
+
+	public int? star_cost { get; set; }
+
+	public bool has_star_cost_x { get; set; }
+
 	public bool is_upgraded { get; set; }
 
 	public bool? can_play { get; set; }
@@ -129,9 +162,23 @@ public sealed class FullRunApiCardOption
 
 	public string? description { get; set; }
 
+	public bool gains_block { get; set; }
+
+	public bool has_turn_end_in_hand_effect { get; set; }
+
+	public bool has_on_draw_effect { get; set; }
+
+	public bool exhaust_on_next_play { get; set; }
+
 	public List<uint> valid_target_ids { get; set; } = new List<uint>();
 
 	public List<string> keywords { get; set; } = new List<string>();
+
+	public List<string> tags { get; set; } = new List<string>();
+
+	public List<string> hover_tip_ids { get; set; } = new List<string>();
+
+	public List<FullRunApiDynamicVar> dynamic_vars { get; set; } = new List<FullRunApiDynamicVar>();
 }
 
 public sealed class FullRunApiPotionState
@@ -166,6 +213,10 @@ public sealed class FullRunApiRelicOption
 	public string? description { get; set; }
 
 	public int? counter { get; set; }
+
+	public bool is_used_up { get; set; }
+
+	public string? status { get; set; }
 }
 
 public sealed class FullRunApiPlayerState
@@ -280,6 +331,12 @@ public sealed class FullRunApiHandSelectState
 
 	public bool can_confirm { get; set; }
 
+	public bool can_cancel { get; set; }
+
+	public int selected_count { get; set; }
+
+	public int remaining_select { get; set; }
+
 	public List<FullRunApiCardOption> cards { get; set; } = new List<FullRunApiCardOption>();
 
 	public List<FullRunApiCardOption> selected_cards { get; set; } = new List<FullRunApiCardOption>();
@@ -299,6 +356,10 @@ public sealed class FullRunApiCombatCardSelectionState
 
 	public bool can_cancel { get; set; }
 
+	public int selected_count { get; set; }
+
+	public int remaining_select { get; set; }
+
 	public List<FullRunApiCardOption> selectable_cards { get; set; } = new List<FullRunApiCardOption>();
 
 	public List<FullRunApiCardOption> selected_cards { get; set; } = new List<FullRunApiCardOption>();
@@ -532,6 +593,12 @@ public sealed class FullRunApiState
 
 	public string? run_outcome { get; set; }
 
+	public int? training_revival_budget { get; set; }
+
+	public int training_revivals_used { get; set; }
+
+	public int training_player_hp_lost { get; set; }
+
 	public FullRunApiRun run { get; set; } = new FullRunApiRun();
 
 	public List<FullRunApiAction> legal_actions { get; set; } = new List<FullRunApiAction>();
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationChoiceBridge.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationChoiceBridge.cs
index 29357a3..8843304 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationChoiceBridge.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationChoiceBridge.cs
@@ -135,27 +135,35 @@ internal sealed class FullRunSimulationChoiceBridge : ICombatChoiceAdapter, IHan
 
 		public IReadOnlyList<CardModel> SelectedCards => _selectedCards;
 
-		public IEnumerable<CardModel> SelectableCards => Options.Where((CardModel card) => !_selectedCards.Contains(card));
+		public bool CanSelectMore => _selectedCards.Count < Prefs.MaxSelect;
 
-		public IEnumerable<(int ChoiceIndex, CardModel Card)> SelectableChoices => Options.Select((CardModel card, int index) => (ChoiceIndex: index, Card: card)).Where((valueTuple) => !_selectedCards.Contains(valueTuple.Card));
+		public IEnumerable<CardModel> SelectableCards => CanSelectMore
+			? Options.Where((CardModel card) => !_selectedCards.Contains(card))
+			: Enumerable.Empty<CardModel>();
+
+		public IEnumerable<(int ChoiceIndex, CardModel Card)> SelectableChoices => CanSelectMore
+			? Options.Select((CardModel card, int index) => (ChoiceIndex: index, Card: card)).Where((valueTuple) => !_selectedCards.Contains(valueTuple.Card))
+			: Enumerable.Empty<(int ChoiceIndex, CardModel Card)>();
+
+		public IEnumerable<(int ChoiceIndex, CardModel Card)> SelectedChoices => Options
+			.Select((CardModel card, int index) => (ChoiceIndex: index, Card: card))
+			.Where((valueTuple) => _selectedCards.Contains(valueTuple.Card));
 
 		public bool CanConfirm => _selectedCards.Count >= Prefs.MinSelect && _selectedCards.Count <= Prefs.MaxSelect;
 
-		public void Select(CardModel card)
+		public bool Select(CardModel card)
 		{
-			if (_selectedCards.Contains(card))
-			{
-				return;
-			}
-			if (Mode == "UpgradeSelect" && _selectedCards.Count > 0)
-			{
-				_selectedCards.Clear();
-			}
-			else if (_selectedCards.Count >= Prefs.MaxSelect && _selectedCards.Count > 0)
+			if (_selectedCards.Contains(card) || !CanSelectMore)
 			{
-				_selectedCards.RemoveAt(_selectedCards.Count - 1);
+				return false;
 			}
 			_selectedCards.Add(card);
+			return true;
+		}
+
+		public bool Deselect(CardModel card)
+		{
+			return _selectedCards.Remove(card);
 		}
 	}
 
@@ -477,6 +485,24 @@ internal sealed class FullRunSimulationChoiceBridge : ICombatChoiceAdapter, IHan
 		return true;
 	}
 
+	public bool TryDeselectHandCard(int handIndex, out string error)
+	{
+		error = "";
+		PendingCardSelection? selection = _pendingCardSelection;
+		if (selection == null || !selection.IsHandSelection)
+		{
+			error = "Hand card selection is not active.";
+			return false;
+		}
+		CardModel? card = selection.SelectedCards.FirstOrDefault((CardModel candidate) => CombatTrainingChoiceSnapshotBuilder.GetHandIndex(candidate) == handIndex);
+		if (card == null || !selection.Deselect(card))
+		{
+			error = $"Hand index {handIndex} is not selected in the current hand selection prompt.";
+			return false;
+		}
+		return true;
+	}
+
 	public bool TrySelectCardChoice(int choiceIndex, out string error)
 	{
 		error = "";
@@ -496,6 +522,24 @@ internal sealed class FullRunSimulationChoiceBridge : ICombatChoiceAdapter, IHan
 		return true;
 	}
 
+	public bool TryDeselectCardChoice(int choiceIndex, out string error)
+	{
+		error = "";
+		PendingCardSelection? selection = _pendingCardSelection;
+		if (selection == null || selection.IsHandSelection)
+		{
+			error = "Card selection is not active.";
+			return false;
+		}
+		(int ChoiceIndex, CardModel Card) choice = selection.SelectedChoices.FirstOrDefault((valueTuple) => valueTuple.ChoiceIndex == choiceIndex);
+		if (choice.Card == null || !selection.Deselect(choice.Card))
+		{
+			error = $"Choice index {choiceIndex} is not selected in the current card selection prompt.";
+			return false;
+		}
+		return true;
+	}
+
 	public bool TryConfirmSelection(out string error)
 	{
 		error = "";
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationDtos.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationDtos.cs
index 6f1ecf4..10841df 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationDtos.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationDtos.cs
@@ -30,6 +30,12 @@ public sealed class SimulationBuildSpec
 	[JsonPropertyName("relics")]
 	public List<SimulationBuildRelicSpec>? Relics { get; set; }
 
+	[JsonPropertyName("additional_relics")]
+	public List<SimulationBuildRelicSpec>? AdditionalRelics { get; set; }
+
+	[JsonPropertyName("training_revival_budget")]
+	public int? TrainingRevivalBudget { get; set; }
+
 	[JsonPropertyName("current_hp")]
 	public int? CurrentHp { get; set; }
 
@@ -106,6 +112,10 @@ public sealed class FullRunSimulationLegalAction
 
 	public int? CardIndex { get; set; }
 
+	public string? SelectionOperation { get; set; }
+
+	public bool? IsSelected { get; set; }
+
 	public int? Slot { get; set; }
 
 	public uint? TargetId { get; set; }
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationStateBuilder.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationStateBuilder.cs
index 4093373..7347075 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationStateBuilder.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulationStateBuilder.cs
@@ -295,6 +295,22 @@ private static string ResolveStateType(RunState runState, AbstractRoom? currentR
 				IsSupported = true
 			});
 		}
+		// Some multi-stage events either clear every option or leave only disabled
+		// previously chosen options after the final choice resolves. The rendered
+		// game closes the room at that point, but pure simulation has no event-layout
+		// button to drive the closeout. Expose that closeout as an explicit model
+		// decision instead of returning a non-terminal state with no legal actions.
+		if (actions.Count == 0)
+		{
+			FullRunSimulationTrace.Write(
+				$"headless_event_legal_actions.exhausted_options id={localEvent.Id.Entry} options={localEvent.CurrentOptions.Count}");
+			actions.Add(new FullRunSimulationLegalAction
+			{
+				Action = "proceed",
+				Label = "Proceed",
+				IsSupported = true
+			});
+		}
 		return actions;
 	}
 
@@ -450,6 +466,21 @@ private static string ResolveStateType(RunState runState, AbstractRoom? currentR
 			actions.Add(new FullRunSimulationLegalAction
 			{
 				Action = "select_card",
+				SelectionOperation = "select",
+				IsSelected = false,
+				Index = card.ChoiceIndex,
+				CardIndex = card.ChoiceIndex,
+				Label = card.Title,
+				IsSupported = true
+			});
+		}
+		foreach (CombatTrainingSelectableCardSnapshot card in selection.SelectedCards)
+		{
+			actions.Add(new FullRunSimulationLegalAction
+			{
+				Action = "deselect_card",
+				SelectionOperation = "deselect",
+				IsSelected = true,
 				Index = card.ChoiceIndex,
 				CardIndex = card.ChoiceIndex,
 				Label = card.Title,
@@ -461,6 +492,7 @@ private static string ResolveStateType(RunState runState, AbstractRoom? currentR
 			actions.Add(new FullRunSimulationLegalAction
 			{
 				Action = "confirm_selection",
+				SelectionOperation = "confirm",
 				Label = "Confirm",
 				IsSupported = true
 			});
@@ -470,6 +502,7 @@ private static string ResolveStateType(RunState runState, AbstractRoom? currentR
 			actions.Add(new FullRunSimulationLegalAction
 			{
 				Action = "cancel_selection",
+				SelectionOperation = "cancel_prompt",
 				Label = "Cancel",
 				IsSupported = true
 			});
@@ -607,6 +640,30 @@ private static string ResolveStateType(RunState runState, AbstractRoom? currentR
 		return actions;
 	}
 
+	private static void AppendCombatSelectionTerminalActions(List<FullRunSimulationLegalAction> actions, bool canConfirm, bool canCancel)
+	{
+		if (canConfirm)
+		{
+			actions.Add(new FullRunSimulationLegalAction
+			{
+				Action = "combat_confirm_selection",
+				SelectionOperation = "confirm",
+				Label = "Confirm",
+				IsSupported = true
+			});
+		}
+		if (canCancel)
+		{
+			actions.Add(new FullRunSimulationLegalAction
+			{
+				Action = "combat_cancel_selection",
+				SelectionOperation = "cancel_prompt",
+				Label = "Cancel",
+				IsSupported = true
+			});
+		}
+	}
+
 	private static List<FullRunSimulationLegalAction> BuildCombatLegalActions(Player localPlayer, FullRunSimulationStateSnapshot snapshot)
 	{
 		List<FullRunSimulationLegalAction> actions = new List<FullRunSimulationLegalAction>();
@@ -654,45 +711,58 @@ private static string ResolveStateType(RunState runState, AbstractRoom? currentR
 					actions.Add(new FullRunSimulationLegalAction
 					{
 						Action = "combat_select_card",
+						SelectionOperation = "select",
+						IsSelected = false,
 						CardIndex = card.HandIndex,
 						Label = card.Title,
 						IsSupported = true
 					});
 				}
-				if (combatState.HandSelection.CanConfirm)
+				foreach (CombatTrainingHandCardSnapshot card in combatState.HandSelection.SelectedCards)
 				{
 					actions.Add(new FullRunSimulationLegalAction
 					{
-						Action = "combat_confirm_selection",
-						Label = "Confirm",
+						Action = "combat_deselect_card",
+						SelectionOperation = "deselect",
+						IsSelected = true,
+						CardIndex = card.HandIndex,
+						Label = card.Title,
 						IsSupported = true
 					});
 				}
+				AppendCombatSelectionTerminalActions(actions, combatState.HandSelection.CanConfirm, combatState.HandSelection.Cancelable);
 				return actions;
 			}
 			if (combatState.IsCardSelectionActive && combatState.CardSelection != null)
 			{
-				foreach (CombatTrainingSelectableCardSnapshot card2 in combatState.CardSelection.SelectableCards)
+				foreach (CombatTrainingSelectableCardSnapshot card in combatState.CardSelection.SelectableCards)
 				{
 					actions.Add(new FullRunSimulationLegalAction
 					{
 						Action = "combat_select_card",
-						CardIndex = card2.ChoiceIndex,
-						Label = card2.Title,
+						SelectionOperation = "select",
+						IsSelected = false,
+						CardIndex = card.ChoiceIndex,
+						Label = card.Title,
 						IsSupported = true
 					});
 				}
-				if (combatState.CardSelection.CanConfirm)
+				foreach (CombatTrainingSelectableCardSnapshot card in combatState.CardSelection.SelectedCards)
 				{
 					actions.Add(new FullRunSimulationLegalAction
 					{
-						Action = "combat_confirm_selection",
-						Label = "Confirm",
+						Action = "combat_deselect_card",
+						SelectionOperation = "deselect",
+						IsSelected = true,
+						CardIndex = card.ChoiceIndex,
+						Label = card.Title,
 						IsSupported = true
 					});
 				}
+				AppendCombatSelectionTerminalActions(actions, combatState.CardSelection.CanConfirm, combatState.CardSelection.Cancelable);
 				return actions;
 			}
+
 			if (!combatState.IsPlayPhase || combatState.PlayerActionsDisabled || combatState.IsActionQueueRunning)
 			{
 				Godot.GD.PrintErr($"[BuildCombatLegalActions] Returning empty: IsPlayPhase={combatState.IsPlayPhase} PlayerActionsDisabled={combatState.PlayerActionsDisabled} IsActionQueueRunning={combatState.IsActionQueueRunning} PlayerHP={combatState.Player?.CurrentHp}");
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulatorRuntimeFacade.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulatorRuntimeFacade.cs
index 217a7c3..a7d1ae3 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulatorRuntimeFacade.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulatorRuntimeFacade.cs
@@ -763,6 +763,24 @@ public sealed class FullRunSimulatorRuntimeFacade : IFullRunRuntimeFacade, IDisp
 					Accepted = true,
 					State = await WaitForStateChangeAsync(state)
 				};
+			case "deselect_card":
+				if (state.StateType != "card_select")
+				{
+					return BuildRejected("deselect_card is only valid from card_select.", "full_run_invalid_state");
+				}
+				if (!action.Index.HasValue)
+				{
+					return BuildRejected("deselect_card requires an index.", "full_run_action_required");
+				}
+				if (!FullRunSimulationChoiceBridge.Instance.TryDeselectCardChoice(action.Index.Value, out string cardDeselectError))
+				{
+					return BuildRejected(cardDeselectError, "full_run_card_deselect_not_found");
+				}
+				return new FullRunSimulationStepResult
+				{
+					Accepted = true,
+					State = await WaitForStateChangeAsync(state)
+				};
 			case "confirm_selection":
 				if (state.StateType != "card_select")
 				{
@@ -826,7 +844,9 @@ public sealed class FullRunSimulatorRuntimeFacade : IFullRunRuntimeFacade, IDisp
 			case "play_card":
 			case "end_turn":
 			case "combat_select_card":
+			case "combat_deselect_card":
 			case "combat_confirm_selection":
+			case "combat_cancel_selection":
 			case "select_card_option":
 			case "use_potion":
 				return await StepCombatAsync(state, actionType, action);
@@ -1155,6 +1175,13 @@ public sealed class FullRunSimulatorRuntimeFacade : IFullRunRuntimeFacade, IDisp
 					HandIndex = action.CardIndex ?? action.HandIndex ?? action.Index,
 					ChoiceIndex = action.CardIndex ?? action.HandIndex ?? action.Index
 				};
+			case "combat_deselect_card":
+				return new CombatTrainingActionRequest
+				{
+					Type = state.StateType == "hand_select" ? CombatTrainingActionType.DeselectHandCard : CombatTrainingActionType.DeselectCardChoice,
+					HandIndex = action.CardIndex ?? action.HandIndex ?? action.Index,
+					ChoiceIndex = action.CardIndex ?? action.HandIndex ?? action.Index
+				};
 			case "select_card_option":
 				return new CombatTrainingActionRequest
 				{
@@ -1166,6 +1193,11 @@ public sealed class FullRunSimulatorRuntimeFacade : IFullRunRuntimeFacade, IDisp
 				{
 					Type = CombatTrainingActionType.ConfirmSelection
 				};
+			case "combat_cancel_selection":
+				return new CombatTrainingActionRequest
+				{
+					Type = CombatTrainingActionType.CancelSelection
+				};
 			case "use_potion":
 				return new CombatTrainingActionRequest
 				{
@@ -2697,10 +2729,21 @@ public sealed class FullRunSimulatorRuntimeFacade : IFullRunRuntimeFacade, IDisp
 			return false;
 		}
 		EventModel localEvent = eventRoom.LocalMutableEvent;
-		if (!localEvent.IsFinished && !localEvent.CurrentOptions.Any(static option => option.IsProceed && !option.IsLocked))
+		bool hasProceedOption = localEvent.CurrentOptions.Any(
+			static option => option.IsProceed && !option.IsLocked);
+		bool hasSelectableOption = localEvent.CurrentOptions.Any(
+			static option => !option.IsLocked
+				&& (!option.WasChosen || !FullRunUpstreamCompat.IsDisableOnChosen(option)));
+		bool exhaustedOptions = !hasSelectableOption;
+		if (!localEvent.IsFinished && !hasProceedOption && !exhaustedOptions)
 		{
 			return false;
 		}
+		if (exhaustedOptions && !localEvent.IsFinished && !hasProceedOption)
+		{
+			FullRunSimulationTrace.Write(
+				$"full_run.event.proceed_exhausted_options id={localEvent.Id.Entry} options={localEvent.CurrentOptions.Count}");
+		}
 		_forceMapView = true;
 		return true;
 	}
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/SimulationBuildSupport.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/SimulationBuildSupport.cs
index 658be06..3ed8f30 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/SimulationBuildSupport.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/SimulationBuildSupport.cs
@@ -29,6 +29,8 @@ internal static class SimulationBuildSupport
 
 		return build.Deck != null
 			|| build.Relics != null
+			|| build.AdditionalRelics != null
+			|| build.TrainingRevivalBudget.HasValue
 			|| build.CurrentHp.HasValue
 			|| build.MaxHp.HasValue
 			|| build.MaxEnergy.HasValue
@@ -71,6 +73,7 @@ internal static class SimulationBuildSupport
 
 	public static void ApplyToPlayerIfRequested(Player player, SimulationBuildSpec? build)
 	{
+		TrainingRevivalController.Configure(build?.TrainingRevivalBudget);
 		if (!HasOverrides(build))
 		{
 			return;
@@ -89,6 +92,23 @@ internal static class SimulationBuildSupport
 			AddDiscoveredRelics(save, save.Relics);
 		}
 
+		if (build.AdditionalRelics != null)
+		{
+			List<SerializableRelic> additions = build.AdditionalRelics
+				.Select(CreateSerializableRelic)
+				.ToList();
+			HashSet<ModelId?> ownedIds = save.Relics.Select(static relic => relic.Id).ToHashSet();
+			foreach (SerializableRelic addition in additions)
+			{
+				RelicModel canonical = ModelDb.GetById<RelicModel>(addition.Id!);
+				if (canonical.IsStackable || ownedIds.Add(addition.Id))
+				{
+					save.Relics.Add(addition);
+				}
+			}
+			AddDiscoveredRelics(save, additions);
+		}
+
 		if (build.MaxHp.HasValue)
 		{
 			save.MaxHp = Math.Max(1, build.MaxHp.Value);
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/TrainingRevivalController.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/TrainingRevivalController.cs
new file mode 100644
index 0000000..6dacd8a
--- /dev/null
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/TrainingRevivalController.cs
@@ -0,0 +1,62 @@
+using System;
+using MegaCrit.Sts2.Core.Entities.Creatures;
+
+namespace MegaCrit.Sts2.Core.Simulation;
+
+/// <summary>
+/// Simulator-only accounting and budget control for native-revival preheat.
+/// A null budget disables the training override, -1 is unlimited, and any
+/// non-negative value is the total number of native Lizard Tail revivals that
+/// may fire in the current reset episode, including an entire full run.
+/// </summary>
+public static class TrainingRevivalController
+{
+	private static int? _revivalBudget;
+
+	public static bool IsEnabled => _revivalBudget.HasValue;
+
+	public static int? RevivalBudget => _revivalBudget;
+
+	public static int RevivalsUsed { get; private set; }
+
+	public static int PlayerHpLost { get; private set; }
+
+	public static bool CanRevive => IsEnabled
+		&& (_revivalBudget == -1 || RevivalsUsed < _revivalBudget);
+
+	public static void Configure(int? revivalBudget)
+	{
+		if (revivalBudget.HasValue && revivalBudget.Value < -1)
+		{
+			throw new InvalidOperationException(
+				"training_revival_budget must be null, -1, or a non-negative integer."
+			);
+		}
+
+		_revivalBudget = revivalBudget;
+		RevivalsUsed = 0;
+		PlayerHpLost = 0;
+	}
+
+	public static void RecordRevival()
+	{
+		if (!CanRevive)
+		{
+			throw new InvalidOperationException(
+				"A training revival fired after its configured budget was exhausted."
+			);
+		}
+
+		RevivalsUsed = checked(RevivalsUsed + 1);
+	}
+
+	public static void RecordHpLoss(Creature creature, int amount)
+	{
+		if (!IsEnabled || !creature.IsPlayer || amount <= 0)
+		{
+			return;
+		}
+
+		PlayerHpLost = checked(PlayerHpLost + amount);
+	}
+}
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingActionType.cs b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingActionType.cs
index 981b065..182d1f0 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingActionType.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingActionType.cs
@@ -8,5 +8,7 @@ public enum CombatTrainingActionType
 	ConfirmSelection = 3,
 	CancelSelection = 4,
 	SelectCardChoice = 5,
-	UsePotion = 6
+	UsePotion = 6,
+	DeselectHandCard = 7,
+	DeselectCardChoice = 8
 }
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingChoiceAdapter.cs b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingChoiceAdapter.cs
index be21d69..2fab22c 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingChoiceAdapter.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingChoiceAdapter.cs
@@ -9,6 +9,7 @@ using MegaCrit.Sts2.Core.Entities.Cards;
 using MegaCrit.Sts2.Core.Entities.Creatures;
 using MegaCrit.Sts2.Core.Entities.Multiplayer;
 using MegaCrit.Sts2.Core.Localization;
+using MegaCrit.Sts2.Core.Localization.DynamicVars;
 using MegaCrit.Sts2.Core.Models;
 using MegaCrit.Sts2.Core.Nodes.Cards.Holders;
 using MegaCrit.Sts2.Core.Nodes.CommonUi;
@@ -31,8 +32,12 @@ public interface ICombatChoiceAdapter
 
 	bool TrySelectHandCard(int handIndex, out string error);
 
+	bool TryDeselectHandCard(int handIndex, out string error);
+
 	bool TrySelectCardChoice(int choiceIndex, out string error);
 
+	bool TryDeselectCardChoice(int choiceIndex, out string error);
+
 	bool TryConfirmSelection(out string error);
 
 	bool TryCancelSelection(out string error);
@@ -72,6 +77,7 @@ internal static class CombatTrainingChoiceSnapshotBuilder
 			EnergyCost = NormalizeApiCardCost(card.EnergyCost.GetWithModifiers(CostModifiers.All), card.EnergyCost.CostsX),
 			CostsX = card.EnergyCost.CostsX,
 			StarCost = card.GetStarCostWithModifiers(),
+			HasStarCostX = card.HasStarCostX,
 			TargetType = card.TargetType,
 			CanPlay = card.CanPlay(),
 			RequiresTarget = CardRequiresTarget(card),
@@ -80,10 +86,140 @@ internal static class CombatTrainingChoiceSnapshotBuilder
 			Description = SafeCardDescription(card),
 			Keywords = SafeCardKeywords(card),
 			IsUpgraded = card.IsUpgraded,
-			GainsBlock = card.GainsBlock
+			GainsBlock = card.GainsBlock,
+			HasTurnEndInHandEffect = card.HasTurnEndInHandEffect,
+			HasOnDrawEffect = HasOnDrawEffect(card),
+			ExhaustOnNextPlay = card.ExhaustOnNextPlay,
+			Tags = SafeCardTags(card),
+			HoverTipIds = SafeCardHoverTipIds(card),
+			DynamicVars = BuildDynamicVarSnapshots(card)
+		};
+	}
+
+	internal static List<CombatTrainingDynamicVarSnapshot> BuildDynamicVarSnapshots(CardModel card)
+	{
+		try
+		{
+			return card.DynamicVars.Values.Select(BuildDynamicVarSnapshot).ToList();
+		}
+		catch
+		{
+			return new List<CombatTrainingDynamicVarSnapshot>();
+		}
+	}
+
+	private static CombatTrainingDynamicVarSnapshot BuildDynamicVarSnapshot(DynamicVar dynamicVar)
+	{
+		Type runtimeType = dynamicVar.GetType();
+		Type? powerVarType = FindGenericBase(runtimeType, typeof(PowerVar<>));
+		return new CombatTrainingDynamicVarSnapshot
+		{
+			Name = dynamicVar.Name,
+			VarType = runtimeType.Name,
+			Family = DynamicVarFamily(dynamicVar, powerVarType),
+			PowerType = powerVarType?.GetGenericArguments()[0].Name,
+			ValueProps = DynamicVarValueProps(dynamicVar),
+			BaseValue = dynamicVar.BaseValue,
+			EnchantedValue = dynamicVar.EnchantedValue,
+			PreviewValue = dynamicVar.PreviewValue,
+			IntValue = dynamicVar.IntValue,
+			WasJustUpgraded = dynamicVar.WasJustUpgraded
+		};
+	}
+
+	private static Type? FindGenericBase(Type runtimeType, Type genericDefinition)
+	{
+		for (Type? type = runtimeType; type != null; type = type.BaseType)
+		{
+			if (type.IsGenericType && type.GetGenericTypeDefinition() == genericDefinition)
+			{
+				return type;
+			}
+		}
+		return null;
+	}
+
+	private static string DynamicVarFamily(DynamicVar dynamicVar, Type? powerVarType)
+	{
+		if (powerVarType != null)
+		{
+			return "power";
+		}
+		return dynamicVar switch
+		{
+			DamageVar or CalculatedDamageVar or ExtraDamageVar or OstyDamageVar => "damage",
+			BlockVar or CalculatedBlockVar => "block",
+			CardsVar => "cards",
+			EnergyVar => "energy",
+			RepeatVar => "repeat",
+			HpLossVar => "hp_loss",
+			HealVar => "heal",
+			MaxHpVar => "max_hp",
+			GoldVar => "gold",
+			StarsVar => "stars",
+			ForgeVar => "forge",
+			SummonVar => "summon",
+			BoolVar => "boolean",
+			StringVar => "string",
+			_ => "value"
+		};
+	}
+
+	private static string? DynamicVarValueProps(DynamicVar dynamicVar)
+	{
+		return dynamicVar switch
+		{
+			DamageVar damage => damage.Props.ToString(),
+			BlockVar block => block.Props.ToString(),
+			CalculatedDamageVar damage => damage.Props.ToString(),
+			CalculatedBlockVar block => block.Props.ToString(),
+			OstyDamageVar damage => damage.Props.ToString(),
+			_ => null
 		};
 	}
 
+	internal static bool HasOnDrawEffect(CardModel card)
+	{
+		try
+		{
+			Type? declaringType = card.GetType().GetMethod(nameof(AbstractModel.AfterCardDrawn))?.DeclaringType;
+			return declaringType != null && declaringType != typeof(AbstractModel);
+		}
+		catch
+		{
+			return false;
+		}
+	}
+
+	internal static List<string> SafeCardTags(CardModel card)
+	{
+		try
+		{
+			return card.Tags.Select(static tag => tag.ToString()).ToList();
+		}
+		catch
+		{
+			return new List<string>();
+		}
+	}
+
+	internal static List<string> SafeCardHoverTipIds(CardModel card)
+	{
+		try
+		{
+			return card.HoverTips
+				.Select(static hoverTip => hoverTip.Id)
+				.Where(static id => !string.IsNullOrWhiteSpace(id))
+				.Distinct(StringComparer.Ordinal)
+				.OrderBy(static id => id, StringComparer.Ordinal)
+				.ToList();
+		}
+		catch
+		{
+			return new List<string>();
+		}
+	}
+
 	private static string SafeCardDescription(CardModel card)
 	{
 		try
@@ -162,10 +298,19 @@ internal static class CombatTrainingChoiceSnapshotBuilder
 			EnergyCost = card.EnergyCost.GetWithModifiers(CostModifiers.All),
 			CostsX = card.EnergyCost.CostsX,
 			StarCost = card.GetStarCostWithModifiers(),
+			HasStarCostX = card.HasStarCostX,
 			TargetType = card.TargetType,
 			SourcePile = card.Pile?.Type.ToString() ?? PileType.None.ToString(),
 			IsUpgraded = card.IsUpgraded,
-			IsUpgradable = card.IsUpgradable
+			IsUpgradable = card.IsUpgradable,
+			GainsBlock = card.GainsBlock,
+			HasTurnEndInHandEffect = card.HasTurnEndInHandEffect,
+			HasOnDrawEffect = HasOnDrawEffect(card),
+			ExhaustOnNextPlay = card.ExhaustOnNextPlay,
+			Tags = SafeCardTags(card),
+			Keywords = SafeCardKeywords(card),
+			HoverTipIds = SafeCardHoverTipIds(card),
+			DynamicVars = BuildDynamicVarSnapshots(card)
 		};
 	}
 
@@ -249,12 +394,39 @@ internal sealed class UiCombatChoiceAdapter : ICombatChoiceAdapter
 		return true;
 	}
 
+	public bool TryDeselectHandCard(int handIndex, out string error)
+	{
+		error = "";
+		if (!_hand.IsInCardSelection)
+		{
+			error = "Hand card selection is not active.";
+			return false;
+		}
+		NSelectedHandCardContainer? selectedContainer = _hand.GetNodeOrNull<NSelectedHandCardContainer>("%SelectedHandCardContainer");
+		NCardHolder? holder = selectedContainer?.Holders.FirstOrDefault(candidate =>
+			candidate.CardNode != null &&
+			CombatTrainingChoiceSnapshotBuilder.GetHandIndex(candidate.CardNode.Model) == handIndex);
+		if (holder == null)
+		{
+			error = $"Hand index {handIndex} is not selected in the current hand selection prompt.";
+			return false;
+		}
+		holder.EmitSignal(NCardHolder.SignalName.Pressed, holder);
+		return true;
+	}
+
 	public bool TrySelectCardChoice(int choiceIndex, out string error)
 	{
 		error = "CardSelection is not supported by the UI-backed combat choice adapter yet.";
 		return false;
 	}
 
+	public bool TryDeselectCardChoice(int choiceIndex, out string error)
+	{
+		error = "CardSelection is not supported by the UI-backed combat choice adapter yet.";
+		return false;
+	}
+
 	public bool TryConfirmSelection(out string error)
 	{
 		error = "";
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingDtos.cs b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingDtos.cs
index 245c978..b5a40fa 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingDtos.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingDtos.cs
@@ -133,6 +133,34 @@ public sealed class CombatTrainingPowerSnapshot
 	public int Amount { get; set; }
 }
 
+/// <summary>
+/// A value emitted by the card model itself.  This is deliberately a generic
+/// runtime fact rather than a hand-maintained card-effect table: new cards and
+/// upgraded values flow through the same contract automatically.
+/// </summary>
+public sealed class CombatTrainingDynamicVarSnapshot
+{
+	public string Name { get; set; } = "";
+
+	public string VarType { get; set; } = "";
+
+	public string Family { get; set; } = "";
+
+	public string? PowerType { get; set; }
+
+	public string? ValueProps { get; set; }
+
+	public decimal BaseValue { get; set; }
+
+	public decimal EnchantedValue { get; set; }
+
+	public decimal PreviewValue { get; set; }
+
+	public int IntValue { get; set; }
+
+	public bool WasJustUpgraded { get; set; }
+}
+
 public sealed class CombatTrainingHandCardSnapshot
 {
 	public int HandIndex { get; set; }
@@ -149,6 +177,8 @@ public sealed class CombatTrainingHandCardSnapshot
 
 	public int StarCost { get; set; }
 
+	public bool HasStarCostX { get; set; }
+
 	public TargetType TargetType { get; set; }
 
 	public bool CanPlay { get; set; }
@@ -165,7 +195,19 @@ public sealed class CombatTrainingHandCardSnapshot
 
 	public bool GainsBlock { get; set; }
 
+	public bool HasTurnEndInHandEffect { get; set; }
+
+	public bool HasOnDrawEffect { get; set; }
+
+	public bool ExhaustOnNextPlay { get; set; }
+
+	public List<string> Tags { get; set; } = new List<string>();
+
 	public List<string> Keywords { get; set; } = new List<string>();
+
+	public List<string> HoverTipIds { get; set; } = new List<string>();
+
+	public List<CombatTrainingDynamicVarSnapshot> DynamicVars { get; set; } = new List<CombatTrainingDynamicVarSnapshot>();
 }
 
 public sealed class CombatTrainingPileSnapshot
@@ -250,6 +292,8 @@ public sealed class CombatTrainingSelectableCardSnapshot
 
 	public int StarCost { get; set; }
 
+	public bool HasStarCostX { get; set; }
+
 	public TargetType TargetType { get; set; }
 
 	public string SourcePile { get; set; } = "";
@@ -257,4 +301,20 @@ public sealed class CombatTrainingSelectableCardSnapshot
 	public bool IsUpgraded { get; set; }
 
 	public bool IsUpgradable { get; set; }
+
+	public bool GainsBlock { get; set; }
+
+	public bool HasTurnEndInHandEffect { get; set; }
+
+	public bool HasOnDrawEffect { get; set; }
+
+	public bool ExhaustOnNextPlay { get; set; }
+
+	public List<string> Tags { get; set; } = new List<string>();
+
+	public List<string> Keywords { get; set; } = new List<string>();
+
+	public List<string> HoverTipIds { get; set; } = new List<string>();
+
+	public List<CombatTrainingDynamicVarSnapshot> DynamicVars { get; set; } = new List<CombatTrainingDynamicVarSnapshot>();
 }
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingEnvService.cs b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingEnvService.cs
index f0f6080..2c977b9 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingEnvService.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingEnvService.cs
@@ -149,6 +149,23 @@ public sealed class CombatTrainingEnvService
 				}
 				break;
 			}
+		case CombatTrainingActionType.DeselectHandCard:
+			{
+				CombatTrainingStepResult? validationFailure = TryValidateSelectionRequest(action, choiceAdapter, requireHandSelection: true);
+				if (validationFailure != null)
+				{
+					return validationFailure;
+				}
+				if (!action.HandIndex.HasValue)
+				{
+					return BuildRejectedResult("DeselectHandCard requires hand_index.");
+				}
+				if (!choiceAdapter.TryDeselectHandCard(action.HandIndex.Value, out string error))
+				{
+					return BuildRejectedResult(error);
+				}
+				break;
+			}
 		case CombatTrainingActionType.SelectCardChoice:
 			{
 				CombatTrainingStepResult? validationFailure = TryValidateSelectionRequest(action, choiceAdapter, requireHandSelection: false);
@@ -166,6 +183,23 @@ public sealed class CombatTrainingEnvService
 				}
 				break;
 			}
+		case CombatTrainingActionType.DeselectCardChoice:
+			{
+				CombatTrainingStepResult? validationFailure = TryValidateSelectionRequest(action, choiceAdapter, requireHandSelection: false);
+				if (validationFailure != null)
+				{
+					return validationFailure;
+				}
+				if (!action.ChoiceIndex.HasValue)
+				{
+					return BuildRejectedResult("DeselectCardChoice requires choice_index.");
+				}
+				if (!choiceAdapter.TryDeselectCardChoice(action.ChoiceIndex.Value, out string error))
+				{
+					return BuildRejectedResult(error);
+				}
+				break;
+			}
 		case CombatTrainingActionType.ConfirmSelection:
 			{
 				CombatTrainingStepResult? validationFailure = TryValidateSelectionRequest(action, choiceAdapter, requireHandSelection: null);
@@ -561,6 +595,7 @@ public sealed class CombatTrainingEnvService
 			IsUpgraded = card.IsUpgraded,
 			CostsX = card.EnergyCost.CostsX,
 			StarCost = card.GetStarCostWithModifiers(),
+			HasStarCostX = card.HasStarCostX,
 			TargetType = card.TargetType,
 			CanPlay = card.CanPlay(),
 			RequiresTarget = CardRequiresTarget(card),
@@ -568,7 +603,13 @@ public sealed class CombatTrainingEnvService
 			CardType = card.Type.ToString(),
 			Description = SafeCardDescription(card),
 			Keywords = SafeCardKeywords(card),
-			GainsBlock = card.GainsBlock
+			GainsBlock = card.GainsBlock,
+			HasTurnEndInHandEffect = card.HasTurnEndInHandEffect,
+			HasOnDrawEffect = CombatTrainingChoiceSnapshotBuilder.HasOnDrawEffect(card),
+			ExhaustOnNextPlay = card.ExhaustOnNextPlay,
+			Tags = CombatTrainingChoiceSnapshotBuilder.SafeCardTags(card),
+			HoverTipIds = CombatTrainingChoiceSnapshotBuilder.SafeCardHoverTipIds(card),
+			DynamicVars = CombatTrainingChoiceSnapshotBuilder.BuildDynamicVarSnapshots(card)
 		};
 	}
 
@@ -712,6 +753,14 @@ public sealed class CombatTrainingEnvService
 		{
 			return BuildRejectedResult("SelectCardChoice requires choice_index.");
 		}
+		if (action.Type == CombatTrainingActionType.DeselectHandCard && !action.HandIndex.HasValue)
+		{
+			return BuildRejectedResult("DeselectHandCard requires hand_index.");
+		}
+		if (action.Type == CombatTrainingActionType.DeselectCardChoice && !action.ChoiceIndex.HasValue)
+		{
+			return BuildRejectedResult("DeselectCardChoice requires choice_index.");
+		}
 		return null;
 	}
 
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingSimulatorChoiceBridge.cs b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingSimulatorChoiceBridge.cs
index 46e3b00..56fc5a1 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingSimulatorChoiceBridge.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Training/CombatTrainingSimulatorChoiceBridge.cs
@@ -37,27 +37,35 @@ internal sealed class CombatTrainingSimulatorChoiceBridge : ICombatChoiceAdapter
 
 		public IReadOnlyList<CardModel> SelectedCards => _selectedCards;
 
-		public IEnumerable<CardModel> SelectableCards => Options.Where((CardModel card) => !_selectedCards.Contains(card));
+		public bool CanSelectMore => _selectedCards.Count < Prefs.MaxSelect;
 
-		public IEnumerable<(int ChoiceIndex, CardModel Card)> SelectableChoices => Options.Select((CardModel card, int index) => (ChoiceIndex: index, Card: card)).Where((valueTuple) => !_selectedCards.Contains(valueTuple.Card));
+		public IEnumerable<CardModel> SelectableCards => CanSelectMore
+			? Options.Where((CardModel card) => !_selectedCards.Contains(card))
+			: Enumerable.Empty<CardModel>();
+
+		public IEnumerable<(int ChoiceIndex, CardModel Card)> SelectableChoices => CanSelectMore
+			? Options.Select((CardModel card, int index) => (ChoiceIndex: index, Card: card)).Where((valueTuple) => !_selectedCards.Contains(valueTuple.Card))
+			: Enumerable.Empty<(int ChoiceIndex, CardModel Card)>();
+
+		public IEnumerable<(int ChoiceIndex, CardModel Card)> SelectedChoices => Options
+			.Select((CardModel card, int index) => (ChoiceIndex: index, Card: card))
+			.Where((valueTuple) => _selectedCards.Contains(valueTuple.Card));
 
 		public bool CanConfirm => _selectedCards.Count >= Prefs.MinSelect && _selectedCards.Count <= Prefs.MaxSelect;
 
-		public void Select(CardModel card)
+		public bool Select(CardModel card)
 		{
-			if (_selectedCards.Contains(card))
-			{
-				return;
-			}
-			if (Mode == "UpgradeSelect" && _selectedCards.Count > 0)
-			{
-				_selectedCards.Clear();
-			}
-			else if (_selectedCards.Count >= Prefs.MaxSelect && _selectedCards.Count > 0)
+			if (_selectedCards.Contains(card) || !CanSelectMore)
 			{
-				_selectedCards.RemoveAt(_selectedCards.Count - 1);
+				return false;
 			}
 			_selectedCards.Add(card);
+			return true;
+		}
+
+		public bool Deselect(CardModel card)
+		{
+			return _selectedCards.Remove(card);
 		}
 	}
 
@@ -155,6 +163,24 @@ internal sealed class CombatTrainingSimulatorChoiceBridge : ICombatChoiceAdapter
 		return true;
 	}
 
+	public bool TryDeselectHandCard(int handIndex, out string error)
+	{
+		error = "";
+		PendingCardSelection? selection = _pendingSelection;
+		if (selection == null || !selection.IsHandSelection)
+		{
+			error = "Hand card selection is not active.";
+			return false;
+		}
+		CardModel? card = selection.SelectedCards.FirstOrDefault((CardModel candidate) => CombatTrainingChoiceSnapshotBuilder.GetHandIndex(candidate) == handIndex);
+		if (card == null || !selection.Deselect(card))
+		{
+			error = $"Hand index {handIndex} is not selected in the current hand selection prompt.";
+			return false;
+		}
+		return true;
+	}
+
 	public bool TrySelectCardChoice(int choiceIndex, out string error)
 	{
 		error = "";
@@ -174,6 +200,24 @@ internal sealed class CombatTrainingSimulatorChoiceBridge : ICombatChoiceAdapter
 		return true;
 	}
 
+	public bool TryDeselectCardChoice(int choiceIndex, out string error)
+	{
+		error = "";
+		PendingCardSelection? selection = _pendingSelection;
+		if (selection == null || selection.IsHandSelection)
+		{
+			error = "Card selection is not active.";
+			return false;
+		}
+		(int ChoiceIndex, CardModel Card) choice = selection.SelectedChoices.FirstOrDefault((valueTuple) => valueTuple.ChoiceIndex == choiceIndex);
+		if (choice.Card == null || !selection.Deselect(choice.Card))
+		{
+			error = $"Choice index {choiceIndex} is not selected in the current card selection prompt.";
+			return false;
+		}
+		return true;
+	}
+
 	public bool TryConfirmSelection(out string error)
 	{
 		error = "";
diff --git a/src/Core/Entities/Creatures/Creature.cs b/src/Core/Entities/Creatures/Creature.cs
index 644a57f..c8c963e 100644
--- a/src/Core/Entities/Creatures/Creature.cs
+++ b/src/Core/Entities/Creatures/Creature.cs
@@ -16,6 +16,7 @@ using MegaCrit.Sts2.Core.Nodes.Rooms;
 using MegaCrit.Sts2.Core.Platform;
 using MegaCrit.Sts2.Core.Random;
 using MegaCrit.Sts2.Core.Runs;
+using MegaCrit.Sts2.Core.Simulation;
 using MegaCrit.Sts2.Core.TestSupport;
 using MegaCrit.Sts2.Core.ValueProps;
 
@@ -377,6 +378,7 @@ public class Creature
 		int currentHp = CurrentHp;
 		int num = (int)Math.Min(amount, 999999999m);
 		CurrentHp = Math.Max(CurrentHp - num, 0);
+		TrainingRevivalController.RecordHpLoss(this, currentHp - CurrentHp);
 		return new DamageResult(this, props)
 		{
 			UnblockedDamage = currentHp - CurrentHp,
diff --git a/src/Core/Models/Relics/LizardTail.cs b/src/Core/Models/Relics/LizardTail.cs
index dffe5c6..e4bdc17 100644
--- a/src/Core/Models/Relics/LizardTail.cs
+++ b/src/Core/Models/Relics/LizardTail.cs
@@ -6,6 +6,7 @@ using MegaCrit.Sts2.Core.Entities.Creatures;
 using MegaCrit.Sts2.Core.Entities.Relics;
 using MegaCrit.Sts2.Core.Localization.DynamicVars;
 using MegaCrit.Sts2.Core.Saves.Runs;
+using MegaCrit.Sts2.Core.Simulation;
 
 namespace MegaCrit.Sts2.Core.Models.Relics;
 
@@ -43,6 +44,10 @@ public sealed class LizardTail : RelicModel
 		{
 			return true;
 		}
+		if (TrainingRevivalController.IsEnabled)
+		{
+			return !TrainingRevivalController.CanRevive;
+		}
 		if (WasUsed)
 		{
 			return true;
@@ -53,8 +58,19 @@ public sealed class LizardTail : RelicModel
 	public override async Task AfterPreventingDeath(Creature creature)
 	{
 		Flash();
+		if (TrainingRevivalController.IsEnabled)
+		{
+			TrainingRevivalController.RecordRevival();
+		}
 		WasUsed = true;
 		decimal amount = Math.Max(1m, (decimal)creature.MaxHp * (base.DynamicVars.Heal.BaseValue / 100m));
 		await CreatureCmd.Heal(creature, amount);
+		if (TrainingRevivalController.CanRevive)
+		{
+			// Rearm only the simulator training copy. The death prevention,
+			// relic flash and 50% native heal above remain the game's own path.
+			WasUsed = false;
+			base.Status = RelicStatus.Normal;
+		}
 	}
 }
