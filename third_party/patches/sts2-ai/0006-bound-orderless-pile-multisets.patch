diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateBuilder.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateBuilder.cs
index 407e08728bc08181b571607ab194a5167c535e13..a1047cd2bf59f946d91e967bd3296bbbd1be55ef 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateBuilder.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateBuilder.cs
@@ -591,20 +591,35 @@ public static class FullRunApiStateBuilder
 	{
 		try
 		{
-			var list = cards
-				.Where(static card => card != null)
-				.Select((card, index) => ToApiCardOption(card, index))
-				.ToList();
-			if (shuffle)
+			// These public piles are orderless surfaces. Project each card first,
+			// then quotient only DTOs whose complete visible facts are byte-for-byte
+			// equal. This keeps payload size proportional to semantic variety rather
+			// than status-card copy count while preserving exact multiplicity.
+			var grouped = new SortedDictionary<string, FullRunApiCardOption>(StringComparer.Ordinal);
+			foreach (CardModel card in cards.Where(static card => card != null))
 			{
-				var rng = new System.Random();
-				for (int i = list.Count - 1; i > 0; i--)
+				FullRunApiCardOption option = ToApiCardOption(card, index: 0);
+				option.quantity = 1;
+				string identity = JsonSerializer.Serialize(option);
+				if (grouped.TryGetValue(identity, out FullRunApiCardOption? existing))
 				{
-					int j = rng.Next(i + 1);
-					(list[i], list[j]) = (list[j], list[i]);
+					existing.quantity = checked((existing.quantity ?? 1) + 1);
 				}
+				else
+				{
+					grouped.Add(identity, option);
+				}
+			}
+
+			// A deterministic identity order reveals no hidden draw order. Retain
+			// compact contiguous indexes for diagnostics and older consumers.
+			List<FullRunApiCardOption> result = grouped.Values.ToList();
+			for (int index = 0; index < result.Count; index++)
+			{
+				result[index].index = index;
 			}
-			return list;
+			_ = shuffle;
+			return result;
 		}
 		catch
 		{
@@ -1250,11 +1265,9 @@ public static class FullRunApiStateBuilder
 	{
 		try
 		{
-			return player.Deck?.Cards?
-				.Where(static card => card != null)
-				.Select((card, index) => ToApiCardOption(card, index))
-				.ToList()
-				?? new List<FullRunApiCardOption>();
+			return player.Deck?.Cards is { } cards
+				? SafeBuildPileCards(cards, shuffle: false)
+				: new List<FullRunApiCardOption>();
 		}
 		catch
 		{
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateDtos.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateDtos.cs
index 9b7c5c80a33de1c0603335f5293ba43f41287fe0..cb7636198b187a04bdc23fa5ac365dd03854a695 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateDtos.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunApiStateDtos.cs
@@ -207,6 +207,10 @@ public sealed class FullRunApiCardOption
 {
 	public int index { get; set; }
 
+	// Exact multiplicity for orderless pile projections. Null is omitted for
+	// hand, selection, action, reward, and shop singleton DTOs.
+	public int? quantity { get; set; }
+
 	public string? id { get; set; }
 
 	public string? name { get; set; }
diff --git a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulatorRuntimeFacade.cs b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulatorRuntimeFacade.cs
index d51be31286c7389fc0f744e12f430f11c4218731..a31221a33d7c8e57af8dfaa05684ac30d16b3d5b 100644
--- a/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulatorRuntimeFacade.cs
+++ b/STS2AI/ENV/Sim/HeadlessSim/Simulation/FullRunSimulatorRuntimeFacade.cs
@@ -3213,7 +3213,7 @@ public sealed class FullRunSimulatorRuntimeFacade : IFullRunRuntimeFacade, IDisp
 		}
 		if (projected.hp != player.Creature.CurrentHp
 			|| projected.max_hp != player.Creature.MaxHp
-			|| projected.deck.Count != player.Deck.Cards.Count)
+			|| projected.deck.Sum(static card => card.quantity ?? 1) != player.Deck.Cards.Count)
 		{
 			throw new InvalidOperationException(
 				$"Non-combat player projection lost persistent fields for state_type={stateType}, orb={orbId}.");
